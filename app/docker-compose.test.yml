services:
  test_db:
    image: postgres:15
    restart: "no"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: database_test
    ports:
      - "54326:5432"
    tmpfs:
      - /var/lib/postgresql/data
